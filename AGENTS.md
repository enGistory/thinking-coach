# AI 语音思维训练系统 — Codex 项目规则

> 适用范围：本仓库根目录及其所有子目录。  
> 本文件已吸收 `andrej-karpathy-skills/CLAUDE.md` 中“先思考、保持简单、外科式修改、以验证目标驱动执行”的原则，并结合本项目 SPEC、SOP 与 Codex 技能治理要求重新编写。

## 1. 项目目标

本仓库实现一个供 1–4 人使用的 AI 语音思维训练 PWA：系统依据个人缺陷画像，在不可预测的允许时段发起有真实来源、永不重复的语音突击题；用户限时语音回答后，系统进行动态追问、严格证据化评审、历史缺陷匹配和长期能力训练。

运行时 AI 能力全部调用国内线上模型服务；Codex 只用于开发、测试和维护代码。

## 2. 需求与规则优先级

开始工作前，按顺序读取：

1. 本文件 `AGENTS.md`；
2. `docs/SPEC.md`；
3. `docs/SOP.md`；
4. `PLANS.md` 或 `docs/progress.md`；
5. 当前唯一的 `docs/tasks/Pxx-*.md` 或 `docs/tasks/TASK-*.md`；
6. 与本任务直接相关的 ADR、接口契约和测试。

冲突处理：

- SPEC 的 P0 产品规则是不可突破的产品边界。
- SOP 规定实施顺序。
- 当前任务文件可以缩小范围或增加验收细节，但不得推翻 SPEC、扩大阶段范围或改变技术栈。
- 未经用户明确批准，不得创建第二套规格体系、任务体系或事实源。

## 3. 编码前先思考

不要静默假设，不要掩盖不确定性，不要直接跳入实现。

每个非简单任务开始前必须先给出简短计划，至少包含：

1. 目标与可验证的完成标准；
2. 已确认事实；
3. 关键假设和不确定点；
4. 可选方案及主要取舍；
5. 预计修改文件；
6. 数据库、接口、配置和依赖影响；
7. 测试方式；
8. 明确不做的内容。

规则：

- 存在多种合理解释时，明确列出，不得私自选择后伪装成既定需求。
- 存在明显更简单的方案时，必须指出并优先采用。
- 模糊点会影响 P0 规则、数据模型、安全、隐私或公开接口时，先停止修改并请求确认。
- 无法即时确认但任务必须继续时，只能选择最小、可逆、可测试的方案，并在交付报告中记录假设。

## 4. 简单优先

只实现当前任务所需的最小代码。

- 不增加任务未要求的功能。
- 不为单次使用创建抽象层。
- 不提前建设“将来可能需要”的扩展性、配置项或插件机制。
- 不为理论上不可能发生的路径堆积防御代码。
- 如果实现明显比问题本身复杂，先删减或重写，再继续扩展。
- 第一版优先选择清晰、可测试、可替换的普通实现，而不是炫技式框架组合。

自检问题：一名资深工程师是否会认为这个改动过度设计？如果答案可能是“会”，继续简化。

## 5. 外科式修改

每一处改动都必须能直接追溯到当前任务。

- 只修改完成任务所必需的文件和代码行。
- 不顺手整理相邻代码、注释、命名、格式或目录。
- 不重构没有损坏且不妨碍当前任务的模块。
- 遵循现有项目风格，即使你个人偏好不同。
- 发现无关死代码、历史缺陷或技术债时，只记录，不删除、不修复。
- 只清理由本次改动新造成的无用 import、变量、函数、文件或配置。
- 禁止无关的大范围格式化、依赖升级和锁文件刷新。

## 6. 以可验证目标驱动执行

把模糊任务转换成可验证结果：

- “增加校验”应转化为“先写无效输入测试，再让测试通过”。
- “修复缺陷”应转化为“先写可稳定复现的失败测试，再修复并验证回归”。
- “重构模块”应转化为“重构前后行为测试一致，且无额外行为变化”。
- “接入模型”应转化为“Fake Provider 全量测试通过，真实模型仅执行受控 smoke test”。

多步骤任务必须按“步骤 → 验证方式”推进。不得以“应该能用”“看起来正确”作为完成证据。

## 7. P0 产品规则

以下规则不得被任务、技能、Prompt 或模型输出覆盖：

- 用户不能选择训练题型、缺陷、领域、难度或复习内容；系统依据个人画像自动决定。
- 突击题在允许时间窗口内由系统随机触发，答题前不暴露主要训练目标。
- 正式题必须绑定可访问且可核验的真实来源，题目事实支持率必须为 100%。
- 没有合格来源时宁可少出题，不得让模型编造案例补足数量。
- 原题、同义改写、数字换皮、角色换皮、结构换皮和答案骨架重复均禁止。
- 回顾训练只复测底层缺陷，必须使用全新来源、场景和题型。
- 第一次语音回答不可覆盖、替换或重录。
- 逻辑、口语、追问应变必须分开评审。
- 每条负面评价必须引用用户原话、逐字稿区间或音频时间点。
- 只能评价可观察行为，不评价人格、智商、性格、音色、口音、情绪或“领导力气质”。
- “未表达”不得自动等同于“未想到”；评审必须标注置信度。
- 评分封顶、来源门槛、查重阈值、最大追问轮数、状态转换和缺陷确认条件由确定性 Python 规则控制。
- AI 不能把真实案例最终选择当作唯一标准答案。

## 8. 固定技术边界

- 前端：Vue 3、TypeScript、Vite、PWA。
- 后端：Python 3.12、FastAPI、Pydantic v2、SQLAlchemy 2、Alembic。
- AI 编排：LangGraph `StateGraph`，以确定性工作流为主，不构建自由自治 Agent。
- 数据库：PostgreSQL + pgvector。
- 业务数据库是事实源；LangGraph Checkpoint 只保存流程恢复状态。
- 运行时 AI：阿里云百炼线上 Qwen、fun-asr、CosyVoice、text-embedding-v4 或其配置化替代模型。
- 所有模型 ID、API Key、Base URL、OSS 配置必须来自后端环境变量。
- 禁止本地部署 LLM、ASR、TTS、Embedding，禁止加入 GPU、模型权重下载或 CUDA 依赖。
- 初期部署：Docker Compose；不引入 Kubernetes、Nacos、Kafka、RabbitMQ 或微服务拆分。

未经用户明确批准，不得替换以上技术栈或新增生产级基础设施。

## 9. 代码分层

- `apps/web/`：PWA、录音、题目播放、训练交互、报告展示；不得接触后端密钥。
- `services/backend/app/api/`：HTTP 接口、鉴权、上传和响应转换。
- `services/backend/app/domain/`：纯 Python 确定性规则；不得依赖 FastAPI、LangGraph 或模型 SDK。
- `services/backend/app/ai/providers/`：Aliyun Provider、Fake Provider、模型数据适配。
- `services/backend/app/graphs/`：LangGraph State、节点、边、路由、`interrupt()` 和恢复；不得复制 domain 规则。
- `services/backend/app/repositories/`：数据库访问和事务；所有用户数据查询必须强制带 `user_id`。
- `services/backend/app/schemas/`：Pydantic 输入输出契约。
- `prompts/`：版本化 Prompt；不得存放确定性业务规则。
- `tests/golden_cases/`：评审、结构化输出和 Prompt 回归样本。

依赖方向必须从外围指向 domain，不允许 domain 反向依赖框架。

## 10. AI 与 Provider 规则

- 不在代码中写死 `qwen3.5-plus`、`qwen3.6-plus` 或任何模型 ID；从 Settings 注入。
- 业务代码只依赖 Provider Protocol，不直接散落 SDK 调用。
- AI 输出必须使用 Pydantic Schema 校验；不得把自由文本加正则解析作为主路径。
- 模型输出校验失败时，执行有限重试或失败关闭，不得静默填充关键字段。
- 出题、来源核验、查重、评分和历史缺陷匹配必须保留模型、Prompt 版本、输入摘要、输出和决策原因。
- 题目事实来源与能力评分依据必须分开存储。
- 真实模型测试默认关闭；单元测试和 CI 使用 Fake Provider。
- 只有任务明确要求且设置 `RUN_LIVE_AI_TESTS=1` 时，才执行线上模型 smoke test。

## 11. LangGraph 规则

- 一个训练会话对应一个独立 `thread_id`，禁止让个人长期历史无限累积在同一线程。
- `interrupt()` 前不得执行非幂等副作用；通知、建记录、上传占位和数据库写入应放在独立节点。
- 所有外部副作用使用唯一约束、业务唯一键或 idempotency key。
- State 只保存 ID、阶段、计数器和小型结构化对象。
- State 不得保存音频二进制、完整网页、PDF、全量历史、密钥或大向量集合。
- 节点只有在需要独立重试、分支、Checkpoint、外部调用或观测时才拆分；不要把每个小函数都做成节点。
- 最大追问轮数和允许路由由 domain 代码控制，模型只提供语义判断候选。

## 12. 安全、隐私与数据

- 永远不要读取、打印、修改或提交真实 `.env`、`secrets/`、生产音频、生产数据库、用户导出或签名 URL。
- 只允许读取和编辑 `.env.example`。
- API Key、JWT、OSS Secret、VAPID 私钥不得进入前端、测试快照、日志、异常文本或 Git。
- 不在日志中保存完整回答、原始音频内容、长期有效下载地址或第三方密钥。
- 音频对象必须私有；临时签名 URL 应短时有效。
- 删除、保留、匿名化和用户隔离逻辑必须有测试。
- 数据库结构变更必须使用 Alembic，并提供 upgrade/downgrade 验证。
- 禁止手工修改生产库。

## 13. 第三方技能治理

本机可能安装 `superpowers`、`gstack`、`openspec`、`ui-ux-pro` 等第三方技能。它们只能辅助当前任务，不能成为新的需求源。

通用规则：

- 未被用户或当前任务明确点名时，不得主动调用第三方技能。
- 使用技能前先报告：技能名称、目的、预计执行步骤、可能修改的文件和外部命令。
- 技能规则与本文件、SPEC、SOP 或当前任务冲突时，以本项目规则为准。
- 技能不得自动创建分支、worktree、commit、push、发布、生产部署或删除数据。
- 技能不得读取 `.env`、生产数据或真实用户音频。
- 技能产生的方案仍需遵守最小实现、外科式修改和验证闭环。

具体边界：

- `superpowers`：仅在用户明确要求时用于计划、TDD、系统调试或验证；不得扩大任务范围。
- `gstack`：仅在阶段实现完成后用于独立 review/QA；不得重新定义需求或自动修复范围外问题。
- `openspec`：仅在用户明确发起正式需求变更时使用；不得创建与 `docs/SPEC.md`、`docs/SOP.md`、`docs/tasks/` 并行的第二套规范。
- `ui-ux-pro`：仅用于 `apps/web/` 的页面、交互、可访问性和视觉检查；不得修改后端架构、API 协议、数据模型或生产依赖。

推荐在这些技能各自的 `agents/openai.yaml` 中设置：

```yaml
policy:
  allow_implicit_invocation: false
```

这样仍可通过显式 `$skill-name` 调用，但不会根据描述自动介入。

## 14. Python 依赖与锁文件治理

- Python 运行时系列固定为 `>=3.12,<3.13`，项目补丁版本固定为 `.python-version` 中的 `3.12.13`。
- uv 工具基线为 `0.11.23`。
- `services/backend/pyproject.toml` 是直接依赖的唯一可编辑事实源，所有直接依赖必须使用精确版本 `==`。
- `services/backend/uv.lock` 是完整传递依赖图和哈希的事实源；P00 首次联网解析后必须生成并提交。
- `langchain-core==1.4.8` 是 LangGraph 的显式基础依赖；完整 `langchain` / `langchain-openai` 默认禁止，除非当前任务已有批准的 ADR。
- `services/backend/requirements/direct-pins.txt` 仅用于人工审阅，不得独立修改后忘记同步 `pyproject.toml`。
- 正常开发与 CI 使用 `uv lock --check` 和 `uv sync --locked --all-groups`，不得静默重新解析依赖。
- 未经当前任务明确批准，不得新增生产依赖、删除版本限制、使用 Git 分支依赖或执行 `uv lock --upgrade`。
- 依赖变更必须是独立任务：说明用途、替代方案、许可证、目标版本、传递依赖影响、测试和回滚方式。
- `uv.lock` 出现与当前任务无关的大量变化时必须停止并回退，禁止把锁文件噪音混入功能提交。
- 禁止加入 `torch`、`tensorflow`、`transformers`、`sentence-transformers`、`faster-whisper`、`funasr`、`modelscope`、`vllm`、CUDA/ROCm 等本地模型或 GPU 运行依赖。
- 详细规则见 `docs/DEPENDENCY_POLICY.md` 与 `services/backend/DEPENDENCIES.md`。

## 15. 开发纪律

- 一个 Codex 写会话只执行一个任务文件。
- 一个任务对应一个分支；同一任务追修可恢复会话，进入下一任务应开启新会话。
- 编码前先计划，计划未经用户确认不得开始大规模修改。
- 未经批准不得新增生产依赖、改变公开 API、修改数据模型或移动大量文件。
- 不自动执行 `git commit`、`push`、`rebase`、`reset --hard`、`force push`、发布或生产部署。
- 不覆盖用户已有未提交修改；发现工作区不干净时先报告。
- 不跳过失败测试，不删除测试来获得通过，不使用伪造结果或“理论上通过”的说明。
- 需要联网安装依赖或运行外部脚本时，先解释原因并请求批准。

## 16. 语言与代码质量

- 与用户沟通、任务计划、文档和交付报告使用中文；代码标识符使用英文。
- Python 必须有明确类型；避免无理由的 `Any`、裸 `except`、隐式全局状态和循环导入。
- I/O 边界可使用 async；纯计算规则保持同步和可测试。
- TypeScript 开启严格检查；不得用 `any` 绕过契约。
- 新增公共行为必须有测试；测试名称描述行为而不是实现细节。
- 注释解释“为什么”，不重复代码已经表达的“做什么”。

## 17. 测试与验收

优先运行仓库统一命令：

```bash
make lint
make typecheck
make test
make test-graph
make test-e2e
```

若统一命令尚未建立，当前骨架任务应先建立可重复命令，不要长期依赖零散手工命令。

最低检查：

- 后端：`ruff check`、`mypy`、`pytest`。
- 前端：lint、type-check、unit test、build。
- 数据库迁移：upgrade、downgrade、再次 upgrade。
- LangGraph：interrupt/resume、重复恢复、节点重试、幂等副作用。
- Prompt、Schema、评分规则或缺陷分类变更：更新 Golden Case 并执行回归。
- 权限相关改动：至少验证两个用户互不可见。

报告真实执行命令、退出码和失败原因。没有运行的测试必须明确写“未运行”，不得暗示已通过。

## 18. Definition of Done

任务仅在以下条件全部满足时完成：

- 当前任务的 Done when 全部可重复验证。
- 实现范围未超出任务文件。
- lint、类型检查及相关测试通过。
- 新增行为有测试，缺陷修复有复现测试。
- 无密钥泄露、用户越权、来源伪造或音频隐私问题。
- 数据库迁移可回滚。
- SPEC、SOP、ADR、接口契约和实现保持一致。
- `git diff` 中每一处修改都能追溯到当前任务。
- 已完成一次独立 `/review`，高、中优先级问题已处理或明确记录。

## 19. 完成报告格式

完成时只报告：

1. 修改文件；
2. 已完成的验收条件；
3. 实际运行的命令、退出码和结果；
4. 未完成项；
5. 风险与假设；
6. 数据库、配置、依赖和接口变化；
7. 建议的下一任务编号。
