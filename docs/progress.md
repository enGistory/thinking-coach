# 开发进度

| 阶段 | 状态 | 分支/提交 | 完成证据 | 剩余风险 |
|---|---|---|---|---|
| P00 工程骨架 | NOT_STARTED |  |  |  |
| P01 云模型配置 | VERIFIED | codex/p01-cloud-providers | 后端 lint、format、mypy、pytest、依赖策略、mock smoke、真实 Qwen/ASR/TTS/Embedding smoke 均通过 | 真实 `.env` 需同步 `.env.example` 的非敏感 AI 配置；已暴露过的 API Key 上线前建议轮换 |
| P02 数据库与账号 | VERIFIED | codex/p02-database-auth | Alembic P02 迁移、邀请注册、登录、JWT/refresh token、USER/ADMIN 权限、训练策略 user_id 隔离、admin failed jobs 脱敏已实现；后端 lint、format、mypy、依赖策略、完整 pytest、显式 Alembic downgrade/upgrade、启动检查、diff 空白检查均通过；review 的中优先级 JWT_SECRET 启动校验问题已修复 | 未提交；存在 Starlette TestClient 与 Alembic path_separator 既有 deprecation warnings；真实生产 JWT_SECRET 需由环境变量配置 |
| P03 语音纵向切片 | VERIFIED | codex/p03-audio-slice | 后端 ruff format/check、mypy、pytest、依赖策略、Alembic downgrade/upgrade 通过；前端 lint、type-check、test、build 通过；MuMu Android 模拟器录音、上传、回放 smoke 通过；/review 发现并修复并发覆盖风险 | 未提交；MuMu 只覆盖 Android 模拟器，不等同 iPhone 或物理真机音质证明；存在既有 Starlette TestClient 与 Alembic path_separator deprecation warnings |
| P04 转写与口语指标 | VERIFIED | codex/p04-transcription-metrics | P04 Alembic 迁移、逐字稿/segment/correction 存储、Aliyun fun-asr + 私有 OSS 适配、转写 job 重试幂等、口语指标、片段级纠错、前端 transcript 展示与点击 seek 已实现；后端 ruff/format/mypy/pytest、依赖策略、Alembic upgrade/downgrade/upgrade、前端 lint/type-check/test/build、uv lock --check、git diff --check、受控 live ASR smoke、/review 与修复回归均通过 | 未 push；live ASR smoke 只覆盖非敏感样例，生产部署前需按目标环境再跑；存在既有 Starlette/httpx 与 Alembic deprecation warnings |
| P05 LangGraph 会话 | VERIFIED | codex/p05-langgraph-session | 计划已保存到 `docs/plans/P05-langgraph-session-plan.md`；实现 LangGraph `StateGraph` + Postgres checkpointer + interrupt/resume、`GRAPH_RESUME` 幂等 job、`GET /state` 与 `POST /resume`、三阶段前端录音恢复；后端 uv lock、依赖策略、ruff、format、mypy、带本地 test DB 的 pytest 全量通过；前端 lint/type-check/test/build 通过；`/review` clean | 未 push；P05 使用开发测试题与 1 轮追问，正式来源出题、严格评审、报告和缺陷记忆留给后续 P06-P09；存在既有 Starlette/httpx 与 Alembic path_separator deprecation warnings |
| P06 严格评审 | VERIFIED | codex/p06-evaluation | 计划已保存到 `docs/plans/P06-evaluation-plan.md`；交接已更新到 `docs/handoffs/P06-evaluation-handoff.md`；已实现评审数据表、P06 Alembic 迁移、结构化评审 Schema、确定性证据复核与评分封顶、版本化 Prompt、`EVALUATE_SESSION` job、worker 评审分支、P05 最终回答后进入 `EVALUATING` 并排队评审；review 发现的证据复核与 prompt/schema 字段问题、迁移回滚问题、`evaluation_issue` 跨 attempt 唯一约束问题和中文应变评分问题均已修复；后端 ruff/format/mypy/pytest、依赖策略、uv lock、前端 lint/type-check/test/build、git diff --check 均通过；本地 PostgreSQL 测试库完成 P06 Alembic upgrade/downgrade/upgrade；MuMu 12 重新完成登录、首答、追问、最终答、转写、评审 job 与报告入库端到端验收 | 未 push；MuMu 端到端验收使用 mock STT/LLM，不等同真实模型评审验收；设置 `RUN_LIVE_AI_TESTS=1` 后因当前进程环境缺少 Aliyun/Qwen/DashScope 必需配置而未执行真实线上 smoke；mock 评审未产生负面 issue，负面评价 quote/start/end 证据由后端单元/集成测试覆盖；存在既有 Starlette/httpx、Alembic path_separator 与 mypy unused override 提示；MuMu 临时验收库 `thinking_mumu_p06_reverify_153327` 保留为本地证据 |
| P07 缺陷经验库 | VERIFIED | HEAD detached | 计划已保存到 `docs/plans/P07-defect-memory-plan.md`；交接已保存到 `docs/handoffs/P07-defect-memory-handoff.md`；已实现 P07 缺陷字典、occurrence/evidence/profile/appeal 表与迁移、P06 issue 幂等同步、五状态画像 Schema、申诉暂停和复核恢复/排除路径、`GET /api/v1/me/defects`、`GET /api/v1/me/defects/{code}/occurrences`、`POST /api/v1/trainings/{id}/appeals`；review 发现并修复空靶标申诉、已复核申诉二次翻转、跨 session attempt 同步、复发 high-priority 重算不幂等四项风险；后端 ruff、format、mypy、pytest、uv lock、依赖策略、git diff --check 通过；本地 PostgreSQL 测试库完成 P07 Alembic upgrade/downgrade/upgrade；MuMu 12 完成登录、首答、追问、最终答、转写、评审、P07 controlled issue 同步、画像/证据 API 和申诉暂停验收；`RUN_LIVE_AI_TESTS=1` 下使用 dev env 覆盖完成 Qwen、Embedding、TTS、FunASR live smoke | 未提交；MuMu 验收使用 mock STT/LLM，FunASR live smoke 使用生成英文测试音频，不等同真实中文录音端到端验收；P06 mock 评审未自然产生负面 issue，P07 画像使用同一 MuMu session 上的 controlled evaluation issue 验证；`improving/stable-improved` 自动进入需等待 P08-P10 目标缺陷复测数据源；存在既有 Starlette/httpx 与 Alembic path_separator deprecation warnings |
| P08 来源与出题 | VERIFIED | codex/p08-source-question | 计划已保存到 `docs/plans/P08-source-question-plan.md`；交接已保存到 `docs/handoffs/P08-source-question-handoff.md`；已实现来源检索/抓取、Claim 提取与验证、READY 题冻结、题目级 Rubric、`PREPARE_QUESTIONS` job、训练绑定 READY 题、完成后 provenance 展示；review/fix 已修复 DNS 解析后私网 SSRF 拦截、P08 Golden Case 回归、provenance 双用户隔离、prepare job 重试幂等 credential、受控 live Bocha smoke 入口和 DB 集成测试 ai_job 外键夹具问题；后端 ruff/format/mypy/pytest、uv lock、依赖策略、P08 DB 集成、P08 Alembic upgrade/downgrade/upgrade、前端 lint/type-check/test/build、git diff --check 均通过；显式设置 `RUN_LIVE_SEARCH_TESTS=1` 与 `BOCHA_API_KEY` 后 live Bocha smoke 通过 | 未提交；live Bocha smoke 只覆盖一次非敏感公开查询，不代表供应商 SLA 或额度；本轮 smoke 使用的 Search API key 曾在会话中显式提供，生产上线前建议轮换；P09 四级防重复和 P10 随机突击/推送仍未实现；存在既有 Starlette/httpx 与 Alembic path_separator deprecation warnings |
| P09 永不重复 | VERIFIED | codex/p09-never-repeat | 计划已保存到 `docs/plans/P09-never-repeat-plan.md`；交接已保存到 `docs/handoffs/P09-never-repeat-handoff.md`；已实现四级防重复、P09 dedupe 审计、duplicate complaint API、题目/报告/缺陷画像作废链路、模板 denylist、补题 job 幂等、前端重复投诉入口；review 发现的跨用户去重污染和重复投诉重复补题问题已修复；`uv lock --check`、依赖策略、ruff、format、mypy、P09 专项测试、完整后端 pytest、P09 Alembic upgrade/downgrade/upgrade、前端 lint/type/test/build、MuMu 12 端到端验收均通过 | 未提交；MuMu 验收使用 mock STT/LLM，不等同真实模型质量验收；真实 Qwen/Embedding live smoke 未运行，按 P09 计划默认不作为 Done 条件；保留既有 Starlette/httpx 与 Alembic path_separator deprecation warnings；临时验收库与截图仅作本地证据 |
| P10 随机突击 | VERIFIED | codex/p10-random-strike | 计划已保存到 `docs/plans/P10-random-strike-plan.md`；交接已保存到 `docs/handoffs/P10-random-strike-handoff.md`；已实现随机突击规则、READY 库存阈值、APScheduler 到期发送、Web Push 订阅 API、中性通知 payload、接受/延期/过期/曝光后 abandon 状态流、PWA 订阅和接受前隐藏题目/来源；review 已修复活跃 session 并发唯一约束、Push endpoint SSRF 防护、随机决策时间源可重放和 `PushManager.subscribe()` reject 受控降级；本轮追修增加 `WEB_PUSH_ALLOW_FAKE_IP_HOSTS` 默认关闭配置，只允许 exact public host 在解析到 198.18.0.0/15 fake-IP 时继续发送，同时支持 PEM VAPID 私钥并让 pywebpush 异常失败关闭；`notificationclick` 已加固为同源 URL 解析、`client.navigate()` 失败后回退 `clients.openWindow()`，并新增 `pushHandler` 专项测试；完成态恢复已支持通知 URL 或本地保存的 session id 在 `/current` 返回 404 后恢复 `COMPLETED` 的 `/state`，并已在 vivo X100 Pro + Chrome 上用 session `659951a4-6a28-4533-a0f1-b61794a8fd55` 重跑通过；前端已用 refresh token 静默恢复登录态，测试/Mock 题文案已改为题目内直接给出已知事实；2026-06-25 最终 `$project-verify` 补跑后端 uv lock、依赖策略、ruff、format、mypy、P10 专项 pytest、完整 pytest、前端 lint/type-check/test/build、`git diff --check` 均通过；物理手机 vivo X100 Pro + Chrome 在临时电脑代理后完成真实发送与完整链路验收：真实 FCM `PushSubscription` 保存成功，`PyWebPushSender` 无 monkeypatch 发送成功，系统通知到达，通知点击可打开带 session 的 PWA URL，接受前题目 `READY/exposed_count=0`，接受后题目 `EXPOSED/exposed_count=1`，三段手机录音均 `UPLOADED`，三段 transcript 均 `SUCCEEDED`，session `3a1f5178-8d99-4629-a156-60c398b3e659` 最终 `COMPLETED`，评审报告 `COMPLETED` | 未提交；MuMu 正向 Push 验收已按 2026-06-24 用户明确指示先忽略，当前保留为非阻塞兼容风险：MuMu 12 无 GMS/Play Services，当前已测 Firefox/Fenix、AOSP Chromium、Fennec F-Droid、IronFox、Iceraven 均无法在 MuMu 内生成网页 `PushSubscription`，但 ntfy + Sunup + UP-Example 已证明 MuMu 非 Google UnifiedPush distributor 本身可用；物理手机直连网络无法访问 FCM 注册 HTTPS API，需临时代理；可信代理 fake-IP 环境需要显式设置 `WEB_PUSH_ALLOW_FAKE_IP_HOSTS=fcm.googleapis.com`，生产默认保持为空；存在既有 Starlette/httpx 与 Alembic path_separator deprecation warnings |
| P11 发布验收 | IN_PROGRESS | codex/p11-release | 已保存计划到 `docs/plans/P11-release-plan.md`；已实现 P11 后端迁移、报告/周报、四类申诉、导出、单次训练删除、账号删除请求与异步 worker、生产 Compose/Nginx/HTTPS 示例、备份/恢复、checkpointer setup 脚本和真实发布 env 预检脚本；前端 `pnpm --dir apps/web lint`、`type-check`、`test`、`build` 通过，P11 API wrapper 专项覆盖 report/appeals/weekly/export/delete/status；后端 `uv run ruff check .`、`uv run ruff format --check .`、`uv run mypy app`、`uv run pytest -q` 通过；新增 P11 发布配置回归测试，覆盖 `infra/env.production.example` 可通过后端启动校验、真实 env/证书/备份路径被 Git 和 Docker build context 忽略、发布 env 预检拒绝占位值并接受非敏感生产形态 env、CLI 输出不打印配置的 secret 值、宿主进程环境变量不会掩盖目标 env 文件错误、生产 Nginx/Compose 保持 HTTPS 跳转、TLS 证书挂载、API 反代、上传大小、音频卷、迁移和 checkpointer setup，并锁定 Nginx 最终镜像阶段只复制 `default.conf` 与前端 `dist` 产物、不带源码、lockfile 或 env 文件；临时 pgvector 测试库中 `tests/test_migrations_p11.py tests/test_release_privacy_p11.py` 通过，并覆盖账号/单次训练删除时的 `model_run`、question/source/fingerprint 向量级残留清理；追加 hardening 后 `tests/test_release_privacy_p11.py` 在临时 pgvector 中 5 项通过，覆盖 report/weekly 跨用户隔离、周报幂等、transcript 申诉接受后报告失效、admin 申诉队列仅保留 OPEN、个人导出包含来源/audit 元数据且不含音频路径/音频内容/签名 URL/模型私有输出/其他用户 session、单条训练和账号删除会清理并返回 ordinary appeals 与 duplicate complaints 计数；本轮继续验证中，后端 ruff/format/mypy/pytest、uv lock、依赖策略、前端 lint/type-check/test/build、privacy API 专项测试、Compose config、`git diff --check` 通过，临时 pgvector 中 P11 迁移+隐私集成 6 项通过；手动 review 复查后收紧 report/state 来源摘要查询的显式 `user_id` 过滤，并在临时 pgvector 中 P11 迁移+隐私集成 7 项通过；继续收紧 duplicate complaint 相似题、随机突击 accept、voice graph 题干读取的 `user_id` 过滤，临时 pgvector 中新增跨用户题目引用回归与重复投诉回归 2 项通过，后端 ruff/format/mypy/pytest 全量通过；本轮新增 `docs/handoffs/P11-release-acceptance-checklist.md` 发布验收清单，备份 dump 生成可移动 `.sha256` sidecar，恢复脚本在 `pg_restore` 前校验 checksum，并把 HomeView 的 P11 报告/申诉/周报/导出/删除用户文案统一为中文；`tests/test_release_config_p11.py` 10 项、`bash -n`、后端 ruff/format/mypy/pytest、前端 lint/type-check/test/build、Compose config、`git diff --check` 通过；继续把 restore checksum 回归从静态检查增强为可执行测试，坏 `.sha256` 会在读取 `infra/env.production` 或触达 Docker/`pg_restore` 前失败，`tests/test_release_config_p11.py` 11 项、后端 ruff/format/mypy/pytest、`bash -n`、`git diff --check` 通过；继续加固账号删除证明，成功删除后 `privacy_deletion_request.user_id_snapshot` 和 request-scoped audit `user_id_snapshot` 清空，audit `target_id` 改为 `user_id_hash`，临时 pgvector 中 P11 迁移+隐私集成 7 项通过，后端 ruff/format/mypy/pytest、uv lock、依赖策略、`git diff --check` 通过，且无残留 `codex-p11-pg-*` 容器；继续补齐 P11 HTTP API 层回归，覆盖报告用户隔离、个人导出脱敏和 transcript correction history、跨用户删除拒绝、单条训练删除、账号删除请求、ADMIN 自删 403 且不创建删除请求/不禁用管理员/不排队删除 job、无 bearer proof 状态查询、错误 proof 关闭、删除后 bearer 失效、admin 队列 403/OPEN 过滤、admin 复核后报告作废/occurrence 排除、周报 API 用户隔离，以及普通申诉+重复投诉合并状态列表，临时 pgvector 中 P11 迁移+隐私集成 12 项通过，后端 ruff/format/mypy/pytest 与 `git diff --check` 通过；备份/恢复演练通过；当前补测 transcript correction 导出和 `transcript_corrections` 删除计数，`tests/test_migrations_p11.py tests/test_release_privacy_p11.py` 12 项、对应 ruff、mypy 和 `git diff --check` 通过；当前补测 `tests/test_release_config_p11.py` 13 项、对应 ruff 和 `git diff --check` 通过 | 未完成真实生产部署、P0 真机 E2E；完整交互式 `/review` 因当前宿主无 AskUserQuestion 工具未运行，仅完成 checklist 手动 review；尚未提交 |

## P11 补充验证记录

2026-06-25 继续补齐账号删除 proof counts：覆盖 refresh token、训练策略、push subscription、来源/题目/claim/vector/rubric/去重审计、缺陷 occurrence/evidence/profile、周报等用户直属业务数据；`uv run ruff check --fix app/repositories/privacy.py tests/test_release_privacy_p11.py`、`uv run ruff format app/repositories/privacy.py tests/test_release_privacy_p11.py`、`uv run ruff check app/repositories/privacy.py tests/test_release_privacy_p11.py`、`uv run mypy app`、临时 pgvector 中 `uv run pytest -q tests/test_migrations_p11.py tests/test_release_privacy_p11.py` 12 项、`git diff --check` 通过；无残留 `codex-p11-pg-*` 容器。

2026-06-25 继续补齐单条训练删除 proof counts：覆盖 orphan question 的 `question_claim_maps`、`question_fingerprints`、`question_rubrics`、`question_sources`、`source_claims`，并同时验证 question-bound 与 session-bound rubric；`uv run ruff format app/repositories/privacy.py tests/test_release_privacy_p11.py`、`uv run ruff check app/repositories/privacy.py tests/test_release_privacy_p11.py`、`uv run mypy app`、临时 pgvector 中 `uv run pytest -q tests/test_migrations_p11.py tests/test_release_privacy_p11.py` 12 项、`git diff --check` 通过；无残留 `codex-p11-pg-*` 容器。

2026-06-25 继续补齐发布产物隐私检查：`git check-ignore infra/env.production infra/certs/fullchain.pem infra/certs/privkey.pem backups/postgres/thinking-test.dump` 通过；`pnpm --dir apps/web build` 通过；`docker compose --env-file infra/env.production.example -f infra/docker-compose.prod.yml config --quiet` 通过；扫描 `apps/web/dist` 中常见 private key、signed URL、Qwen/DashScope/Bocha/Aliyun secret 标记返回 `NO_MATCH`。

2026-06-25 刷新完整本地发布门槛：`uv lock --check`、`uv run python scripts/verify_dependency_policy.py`、`uv run ruff check .`、`uv run ruff format --check .`、`uv run mypy app`、`uv run pytest -q`、`pnpm --dir apps/web lint`、`pnpm --dir apps/web type-check`、`pnpm --dir apps/web test`、`pnpm --dir apps/web build`、`docker compose --env-file infra/env.production.example -f infra/docker-compose.prod.yml config --quiet`、`bash -n infra/scripts/backup-postgres.sh infra/scripts/restore-postgres.sh`、`git diff --check` 均通过；临时 pgvector 中 `uv run pytest -q tests/test_migrations_p11.py tests/test_release_privacy_p11.py` 12 项通过，且无残留 `codex-p11-pg-*` 容器。期间一次 Compose 命令误写为 `infra.env.production.example`，失败原因是找不到 env 文件；已用正确路径重跑通过。

2026-06-25 手动 review 继续加固 source appeal 目标校验：`_source_target_exists()` 现在要求 `training_session.question_id` 对应的 question 仍属于该训练用户，避免 corrupted session 引用下把其他用户的 source/claim 作为申诉目标验证通过；新增 `test_source_appeal_rejects_cross_user_question_reference`。本轮 `.venv\Scripts\ruff.exe format app\repositories\defects.py tests\test_release_privacy_p11.py`、`.venv\Scripts\ruff.exe check app\repositories\defects.py tests\test_release_privacy_p11.py`、`.venv\Scripts\mypy.exe app`、临时 pgvector 中目标测试、临时 pgvector 中 `tests/test_migrations_p11.py tests/test_release_privacy_p11.py` 13 项通过；临时容器已清理。并行 `uv run` 检查在当前 Windows 宿主上超时，后续改用同一 venv 内工具直接执行。

2026-06-25 继续加固账号删除 proof 状态查询：`PrivacyService.deletion_status()` 改用 `secrets.compare_digest` 比较 proof hash，避免无鉴权删除证明查询接口使用普通字符串等值比较；现有账号删除回归通过 monkeypatch 记录正确/错误 proof 均走常量时间比较路径。本轮 `.venv\Scripts\ruff.exe format app\services\privacy.py tests\test_release_privacy_p11.py`、`.venv\Scripts\ruff.exe check app\services\privacy.py tests\test_release_privacy_p11.py`、`.venv\Scripts\mypy.exe app`、临时 pgvector 中目标账号删除测试、临时 pgvector 中 `tests/test_release_privacy_p11.py` 12 项通过；第一次目标测试使用库名 `codex_p11` 被测试夹具拒绝重置，改用 `codex_p11_test` 后通过；临时容器已清理。

2026-06-25 继续加固生产 Nginx HTTPS 响应头：API、静态资源和 SPA fallback 响应均新增 HSTS、`X-Content-Type-Options: nosniff`、`X-Frame-Options: DENY`、`Referrer-Policy: same-origin`，并把 Cache-Control 响应头改为 `always`；`test_release_config_p11.py` 新增回归锁定三个服务位置均有安全头。本轮 `.venv\Scripts\ruff.exe format tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check tests\test_release_config_p11.py`、`docker compose --env-file infra/env.production.example -f infra/docker-compose.prod.yml config --quiet`、`.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 14 项通过。

2026-06-25 继续锁定生产音频私有存储边界：新增发布配置回归证明 Nginx service 不挂载 `audio_data`、不引用 `/data/audio`，且 Nginx 配置不存在静态 `/audio` location，避免生产 HTTPS 前端层直接暴露私有音频目录。本轮 `.venv\Scripts\ruff.exe format tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check tests\test_release_config_p11.py`、`docker compose --env-file infra/env.production.example -f infra/docker-compose.prod.yml config --quiet`、`.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 15 项通过。

2026-06-25 继续锁定生产端口暴露边界：新增发布配置回归证明 Compose 仅 Nginx publish `80:80` 与 `443:443`，Postgres、api、worker、scheduler 均不直接发布 host ports，避免绕过 HTTPS/Nginx 入口直接访问内部服务。本轮 `.venv\Scripts\ruff.exe format tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check tests\test_release_config_p11.py`、`docker compose --env-file infra/env.production.example -f infra/docker-compose.prod.yml config --quiet`、`.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 16 项通过。

2026-06-25 继续补齐账号删除 AI 审计残留证明：账号删除回归新增 user-only `PREPARE_QUESTIONS` job 及其 `model_run`，验证 `delete_ai_jobs_for_user_scope()` 会按 `payload.user_id` 清理不带 session/attempt 的用户级任务记录；删除 proof counts 更新为 `ai_jobs=3`、`model_runs=2`，完成后仅保留当前 `DELETE_ACCOUNT` job。本轮 `.venv\Scripts\ruff.exe format tests\test_release_privacy_p11.py`、`.venv\Scripts\ruff.exe check tests\test_release_privacy_p11.py`、`.venv\Scripts\mypy.exe app`、临时 pgvector 中目标账号删除测试、临时 pgvector 中 `tests/test_release_privacy_p11.py` 12 项通过；临时容器已清理。

2026-06-25 继续加固备份隐私默认值：`backup-postgres.sh` 在创建备份目录和 dump 前设置 `umask 077`，让生成的备份文件默认只对当前用户可读写；发布配置回归锁定 `umask 077` 位于 `mkdir -p` 与 `pg_dump` 之前。本轮 `.venv\Scripts\ruff.exe format tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check tests\test_release_config_p11.py`、`bash -n infra/scripts/backup-postgres.sh infra/scripts/restore-postgres.sh`、`.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 16 项通过。

2026-06-25 继续加固发布 env 预检：`validate_release_env.py` 现在要求生产 `APP_BASE_URL` 与 `WEB_BASE_URL` 使用 HTTPS，并拒绝非空 `WEB_PUSH_ALLOW_FAKE_IP_HOSTS`，避免把 P10 本地 fake-IP 代理放行带入生产；`test_release_config_p11.py` 新增回归覆盖 insecure public base URL 和 fake-IP push allowlist。本轮 `.venv\Scripts\ruff.exe format scripts\validate_release_env.py tests\test_release_config_p11.py`、`docker compose --env-file infra/env.production.example -f infra/docker-compose.prod.yml config --quiet`、`.venv\Scripts\mypy.exe app`、`.venv\Scripts\ruff.exe check --fix tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check scripts\validate_release_env.py tests\test_release_config_p11.py`、`.venv\Scripts\pytest.exe -q tests/test_release_config_p11.py` 18 项、`git diff --check` 通过；首次 targeted ruff 仅发现测试文件 import 排序问题，已自动修复后复跑通过。

2026-06-25 继续加固发布入口 URL 预检：`validate_release_env.py` 现在拒绝 `APP_BASE_URL`/`WEB_BASE_URL` 中的 localhost、`.local`、直接 IP 地址和 URL userinfo，要求生产公网入口使用域名形态；`test_release_config_p11.py` 新增回归覆盖 `https://localhost`、`https://127.0.0.1`、`https://api.local` 和 `https://user:pass@...`。本轮 `.venv\Scripts\ruff.exe format scripts\validate_release_env.py tests\test_release_config_p11.py`、`docker compose --env-file infra/env.production.example -f infra/docker-compose.prod.yml config --quiet`、`.venv\Scripts\ruff.exe check scripts\validate_release_env.py tests\test_release_config_p11.py`、`.venv\Scripts\mypy.exe app`、`.venv\Scripts\pytest.exe -q tests/test_release_config_p11.py` 20 项通过。

2026-06-25 继续补齐备份保留策略：`backup-postgres.sh` 成功生成备份后会裁剪旧 `thinking-*.dump`，至少保留最新 7 个日备份和更早 4 个周快照，并同步删除被裁剪 dump 的 `.sha256`，未知手工 dump 名称保留；新增 `--prune-only` 便于非生产目录演练保留策略。`test_release_config_p11.py` 使用临时目录构造 36 个连续日备份和 sidecar，验证保留/裁剪结果且不读取真实 env、不触达 Docker。本轮 `.venv\Scripts\ruff.exe format tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check tests\test_release_config_p11.py`、`bash -n infra/scripts/backup-postgres.sh infra/scripts/restore-postgres.sh`、`.venv\Scripts\mypy.exe app`、`git diff --check`、`.venv\Scripts\pytest.exe -q tests/test_release_config_p11.py` 21 项、`docker compose --env-file infra/env.production.example -f infra/docker-compose.prod.yml config --quiet` 通过；期间发现 Windows/WSL bash 未继承 Python subprocess 传入的 `BACKUP_DIR` 环境变量，最终测试改为在 bash 命令内设置变量后通过。

2026-06-25 继续加固恢复脚本破坏性边界：`restore-postgres.sh` 在 checksum 通过并读取 env 后，默认拒绝对 `APP_ENV=production` 执行 `pg_restore --clean`，除非显式设置 `RESTORE_ALLOW_PRODUCTION=1`；同时清理 source env 中关键变量的 CRLF 回车，避免 Windows 编辑过的 env 绕过保护。新增回归使用临时假 dump、有效 `.sha256` 和 CRLF 形态 `APP_ENV=production` 假 env，验证脚本在触达 `pg_restore` 前退出。本轮 `bash -n infra/scripts/backup-postgres.sh infra/scripts/restore-postgres.sh`、`.venv\Scripts\ruff.exe check tests\test_release_config_p11.py`、`.venv\Scripts\pytest.exe -q tests/test_release_config_p11.py` 22 项、`.venv\Scripts\mypy.exe app`、`docker compose --env-file infra/env.production.example -f infra/docker-compose.prod.yml config --quiet`、`git diff --check` 通过；首次测试暴露 CRLF 导致 `APP_ENV` 比较未命中，已修复并复跑通过。

2026-06-25 继续加固发布密钥强度预检：`validate_release_env.py` 现在拒绝明显过短的 `POSTGRES_PASSWORD`、`JWT_SECRET`、`LANGGRAPH_AES_KEY` 和 `WEB_PUSH_VAPID_PRIVATE_KEY`，错误只输出变量名，不打印 secret 值；`test_release_config_p11.py` 新增短密钥回归并更新非敏感生产形态假值。本轮 `.venv\Scripts\ruff.exe format scripts\validate_release_env.py tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check scripts\validate_release_env.py tests\test_release_config_p11.py`、`.venv\Scripts\mypy.exe app`、`.venv\Scripts\pytest.exe -q tests/test_release_config_p11.py` 23 项、`docker compose --env-file infra/env.production.example -f infra/docker-compose.prod.yml config --quiet`、`bash -n infra/scripts/backup-postgres.sh infra/scripts/restore-postgres.sh`、`git diff --check` 通过。

2026-06-25 继续加固删除证明语义：单条训练删除和账号删除的 proof counts 现在同时记录 `audio_file_references` 与 `audio_files`，前者表示数据库中被清理的音频引用数，后者表示本地物理文件实际 unlink 成功数，避免物理文件已缺失时把两者混同；新增回归覆盖“DB 有音频引用但文件已不存在”时删除仍清业务数据且 `audio_files=0`。本轮 `.venv\Scripts\ruff.exe format app\services\privacy.py app\repositories\privacy.py tests\test_release_privacy_p11.py`、`.venv\Scripts\ruff.exe check app\services\privacy.py app\repositories\privacy.py tests\test_release_privacy_p11.py`、`.venv\Scripts\mypy.exe app`、临时 pgvector 中 `.venv\Scripts\pytest.exe -q tests\test_release_privacy_p11.py` 13 项、`git diff --check` 通过；临时容器已清理。

2026-06-25 继续推进备份恢复演练：临时 Compose 只启动 `postgres` service，写入 `p11_backup_smoke` 测试表后通过 `infra/scripts/backup-postgres.sh` 生成 dump 与 `.sha256`，删除测试表后通过 `infra/scripts/restore-postgres.sh` 恢复并查询到 `restored`，最后 `docker compose down -v` 清理临时 volume/container。演练中发现 Windows PowerShell 生成的 UTF-8 BOM env 会让 bash `source` 第一行失败，已在备份/恢复脚本中增加 env 文件 BOM 预检，失败时在触达 `pg_dump`/`pg_restore` 前以清晰错误退出；`test_release_config_p11.py` 新增回归覆盖该路径。本轮 `.venv\Scripts\ruff.exe format tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check tests\test_release_config_p11.py`、`bash -n infra/scripts/backup-postgres.sh infra/scripts/restore-postgres.sh`、`.venv\Scripts\pytest.exe -q tests/test_release_config_p11.py` 24 项、真实临时 Compose 备份/恢复演练、`docker compose --env-file infra/env.production.example -f infra/docker-compose.prod.yml config --quiet`、`git diff --check` 通过；临时 `codex-p11-*` 容器已清理。

2026-06-25 继续加固前端删除证明查询隐私：HomeView 删除进度表单的 proof code 输入改为 `type="password"`，并关闭 `autocomplete` 与 `spellcheck`；`privacy.test.ts` 锁定删除状态查询不带 bearer、不把 proof code 放进 URL，只通过 POST body 发送；新增 `HomeView.privacy.test.ts` 用 Vite raw import 静态锁定证明码输入属性，避免引入新的前端测试依赖。本轮 `pnpm --dir apps/web test -- src/api/privacy.test.ts src/views/HomeView.privacy.test.ts` 4 项、`pnpm --dir apps/web lint`、`pnpm --dir apps/web type-check`、`pnpm --dir apps/web build`、`git diff --check` 通过；首次 `type-check` 发现测试直接导入 `node:fs` 与当前 app tsconfig 不兼容，已改为 `HomeView.vue?raw` 后复跑通过。

2026-06-25 继续补齐前端构建产物密钥门禁：新增 `pnpm --dir apps/web scan-dist-secrets`，使用无依赖 Node 脚本扫描 `apps/web/dist`，拒绝服务端 secret 变量名以及当前进程中这些 secret 环境变量的实际值进入前端产物；脚本只输出变量名与文件路径，不打印 secret 值。已将该命令加入 `apps/web/package.json`，用于 P11 发布前证明前端产物不含模型/后端私密配置。本轮 `pnpm --dir apps/web build`、`pnpm --dir apps/web scan-dist-secrets`、`pnpm --dir apps/web lint`、`pnpm --dir apps/web type-check`、`pnpm --dir apps/web test -- src/api/privacy.test.ts src/views/HomeView.privacy.test.ts` 4 项通过；首次 `lint` 发现新 Node 脚本缺少 `console/process/URL` 全局声明，已用文件级声明修复后复跑通过。

2026-06-25 继续补齐 LangGraph checkpointer 发布初始化证据：新增 `tests/test_release_checkpointer_p11.py`，覆盖 `setup_checkpointer.py` 会把 SQLAlchemy driver URL 转成 psycopg 可用的 plain `postgresql://` 连接串，并在临时 pgvector 测试库中实际执行 `setup_checkpointer()` 两次，确认 `checkpoint_migrations`、`checkpoints`、`checkpoint_blobs`、`checkpoint_writes` 均被创建且 setup 幂等。本轮 `.venv\Scripts\ruff.exe check --fix tests\test_release_checkpointer_p11.py`、`.venv\Scripts\ruff.exe format tests\test_release_checkpointer_p11.py`、`.venv\Scripts\ruff.exe check tests\test_release_checkpointer_p11.py`、临时 pgvector 中 `.venv\Scripts\pytest.exe -q tests\test_release_checkpointer_p11.py` 2 项、`git diff --check` 通过；首次临时库测试在 Postgres 刚 ready 时遇到连接被服务端关闭，改用 `psql SELECT 1` 轮询到 SQL 可用后复跑通过，临时容器已清理。

2026-06-25 继续加固错误响应脱敏：`app.core.errors` 现在会过滤 HTTPException detail 中的敏感附加键（如 proof、token、authorization、audio、signed URL、secret、API key 等），避免未来接口误把 proof code、refresh token、私有音频路径或签名 URL 放进 detail 后被统一错误 envelope 回显；普通安全标量字段仍保留。`test_health.py` 新增回归构造包含 `proof_code`、`refresh_token`、`audio_path`、`signed_url` 和 `Authorization` 的错误 detail，验证响应只保留安全字段且不包含敏感值。本轮 `.venv\Scripts\ruff.exe format app\core\errors.py tests\test_health.py`、`.venv\Scripts\ruff.exe check app\core\errors.py tests\test_health.py`、`.venv\Scripts\pytest.exe -q tests\test_health.py` 8 项、`.venv\Scripts\mypy.exe app`、`git diff --check` 通过；pytest 仅保留既有 Starlette TestClient deprecation warning。

2026-06-25 继续加固前端端侧音频隐私：账号删除请求和单条训练删除成功后，现在会 best-effort 清空 IndexedDB `pending-audio` 缓存，并清除内存/本地 pending attempt 引用，避免服务端删除完成后浏览器内仍残留未上传音频 Blob；清理失败不阻断主删除流程。新增 `clearPendingAudioBestEffort()` 与 HomeView 静态回归。本轮 `pnpm --dir apps/web test -- src/audio/pendingAudioStore.test.ts src/views/HomeView.privacy.test.ts` 2 文件 5 项、`pnpm --dir apps/web lint`、`pnpm --dir apps/web type-check`、`pnpm --dir apps/web build`、`pnpm --dir apps/web scan-dist-secrets`、`git diff --check` 通过。

2026-06-25 继续加固音频删除证明边界：单条训练删除和账号删除遇到数据库中指向 `audio_root` 外的异常音频相对路径时，不再删除根目录外文件，也不再中止业务数据删除；proof counts 新增 `audio_file_invalid_references` 与 `audio_file_non_file_references`，同时保留 `audio_file_references` 和实际删除的 `audio_files`。新增回归覆盖单条训练删除与账号删除 worker 均会清除业务数据、保留根外文件并记录无效引用。本轮 `.venv\Scripts\ruff.exe format app\services\privacy.py tests\test_release_privacy_p11.py`、`.venv\Scripts\ruff.exe check app\services\privacy.py tests\test_release_privacy_p11.py`、`.venv\Scripts\mypy.exe app`、临时 pgvector 中新增两项目标测试、临时 pgvector 中 `.venv\Scripts\pytest.exe -q tests\test_release_privacy_p11.py` 15 项、`git diff --check` 通过，且无残留 `codex-p11-pg-*` 容器。

2026-06-25 继续补齐非文件音频引用证明：新增回归覆盖数据库音频引用解析到 `audio_root` 内的目录时，删除流程不 unlink 目录、不阻塞业务数据删除，并在 proof counts 中记录 `audio_file_non_file_references=1`、`audio_files=0`。本轮 `.venv\Scripts\ruff.exe format tests\test_release_privacy_p11.py`、`.venv\Scripts\ruff.exe check tests\test_release_privacy_p11.py`、`.venv\Scripts\mypy.exe app`、临时 pgvector 中目标测试、临时 pgvector 中 `.venv\Scripts\pytest.exe -q tests\test_release_privacy_p11.py` 16 项、`git diff --check` 通过，且无残留 `codex-p11-pg-*` 容器；首次临时库目标测试遇到 Postgres 启动竞态，改用 `psql SELECT 1` 等待 SQL ready 后复跑通过。

2026-06-25 继续加固账号删除状态幂等：`PrivacyService.mark_deletion_failed()` 现在不会把已经 `SUCCEEDED` 的账号删除请求降级为 `FAILED`，避免“业务数据已清除但 proof 状态显示失败”的矛盾状态；新增回归模拟成功删除后又收到失败标记，验证 request 保持 `SUCCEEDED`、`error_code` 为空、不会新增 FAILED audit、proof status 仍为成功。本轮 `.venv\Scripts\ruff.exe format app\services\privacy.py tests\test_release_privacy_p11.py`、`.venv\Scripts\ruff.exe check app\services\privacy.py tests\test_release_privacy_p11.py`、`.venv\Scripts\mypy.exe app`、临时 pgvector 中目标测试、临时 pgvector 中 `.venv\Scripts\pytest.exe -q tests\test_release_privacy_p11.py` 17 项、`git diff --check` 通过，且无残留 `codex-p11-pg-*` 容器；首次目标回归因 audit 事件无排序导致断言不稳定，已改为顺序无关断言后复跑通过。

2026-06-25 继续加固账号删除失败审计脱敏：`PrivacyService.mark_deletion_failed()` 现在只持久化符合 `[A-Z][A-Z0-9_]{0,63}` 的安全错误码，调用方误传异常字符串、URL、路径、token 或 proof code 时统一落为 `DELETION_FAILED`，避免删除失败状态和 `privacy_audit_event.error_code` 保存敏感值；新增回归构造包含一次性 proof code、refresh-token-like 值、音频相对路径、S3-compatible signed URL/signature 和 API-key-like 值的 unsafe error，验证 request/audit 只保留 `DELETION_FAILED`。本轮 `.venv\Scripts\ruff.exe format app\services\privacy.py tests\test_release_privacy_p11.py`、`.venv\Scripts\ruff.exe check app\services\privacy.py tests\test_release_privacy_p11.py`、`.venv\Scripts\mypy.exe app`、临时 pgvector 中目标测试 1 项、临时 pgvector 中账号删除相关测试 3 项、`docker ps -a --filter name=codex-p11-pg-redaction- --format {{.Names}}` 无残留、`git diff --check` 通过；首次 `cmd.exe` 传 env 导致 pytest skip，随后 sync URL 默认 `psycopg2` 失败，最终改用 PowerShell `$env:` 和 `postgresql+psycopg://` 后通过。

2026-06-25 继续加固恢复脚本 checksum 绑定：`restore-postgres.sh` 不再用 `sha256sum -c` 信任 sidecar 内的文件名，而是只读取 sidecar 的 64 位 hash，并强制对命令行传入的 `backup_path` 重新计算比对，避免 sidecar 指向其他文件时绕过待恢复 dump 校验；新增回归覆盖 sidecar hash 属于另一个 dump 时在读取 env 或触达 Docker/`pg_restore` 前失败。本轮 `bash -n infra/scripts/backup-postgres.sh infra/scripts/restore-postgres.sh`、`.venv\Scripts\ruff.exe format tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check tests\test_release_config_p11.py`、Node REPL 直接执行 `.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 25 项、Node REPL 直接执行 `.venv\Scripts\mypy.exe app`、Node REPL 直接执行 `git diff --check` 通过；一次 PowerShell 直接运行 pytest 时触发宿主 pagefile/CLR 错误，改用 Node REPL 直接启动进程后通过。

2026-06-25 继续加固备份失败原子性：`backup-postgres.sh` 现在先写 `${output}.tmp`，`pg_dump` 成功后再 `mv` 到最终 `.dump`，并通过 `trap` 在失败时清理临时文件；retention 参数校验也提前到 dump 前，避免配置错误后留下新备份。新增回归用 Bash 函数注入假 `docker` 模拟 `pg_dump` 失败，验证不会留下最终 dump、`.tmp` 或 `.sha256` 半成品。本轮 `bash -n infra/scripts/backup-postgres.sh infra/scripts/restore-postgres.sh`、`.venv\Scripts\ruff.exe format tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check tests\test_release_config_p11.py`、Node REPL 直接执行 `.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 26 项、Node REPL 直接执行 `.venv\Scripts\mypy.exe app`、Node REPL 直接执行 `git diff --check` 通过；测试夹具最初使用 PATH fake docker 时被 Windows/WSL PATH 展开和可执行位影响，已改为导出 Bash 函数后复跑通过。

2026-06-25 继续加固备份 checksum 原子性：`backup-postgres.sh` 现在要求 `sha256sum` 可用，先为临时 dump 生成临时 `.sha256`，再发布最终 `.dump` 和 `.sha256`；如果 checksum 生成失败，会清理临时 dump/临时 sidecar，若最终 dump 已发布但 sidecar 未发布也会回收最终 dump，避免留下不可校验备份。新增回归用 Bash 函数模拟 `docker` 成功但 `sha256sum` 失败，验证不会留下最终 dump、`.tmp` 或 `.sha256` 半成品。本轮 `bash -n infra/scripts/backup-postgres.sh infra/scripts/restore-postgres.sh`、`.venv\Scripts\ruff.exe format tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check tests\test_release_config_p11.py`、Node REPL 直接执行 `.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 27 项、Node REPL 直接执行 `.venv\Scripts\mypy.exe app`、Node REPL 直接执行 `git diff --check` 通过。

2026-06-25 继续加固恢复脚本 checksum 必要性：`restore-postgres.sh` 现在默认要求待恢复 dump 旁边存在 `.sha256` sidecar，缺失时会在读取 release env 或触达 Docker/`pg_restore` 前失败；只有显式设置 `RESTORE_ALLOW_MISSING_CHECKSUM=1` 才允许 legacy rehearsal 继续，但该覆盖不会绕过 `APP_ENV=production` 的 `RESTORE_ALLOW_PRODUCTION=1` 保护。新增回归覆盖缺失 sidecar 默认失败、覆盖开关继续触发 production guard。本轮 `bash -n infra/scripts/backup-postgres.sh infra/scripts/restore-postgres.sh`、`.venv\Scripts\ruff.exe format tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check tests\test_release_config_p11.py`、Node REPL 直接执行 `.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 29 项、Node REPL 直接执行 `.venv\Scripts\mypy.exe app`、Node REPL 直接执行 `git diff --check` 通过。

2026-06-25 继续加固发布 env 编码预检：`validate_release_env.py` 现在在解析变量前显式拒绝 BOM 和非法 UTF-8 env 文件，避免 Windows 生成的 BOM 文件变成误导性的“首个变量缺失”错误，同时不输出配置值；新增回归覆盖 BOM 文件不会打印 secret 值、非法 UTF-8 fail closed。本轮 `.venv\Scripts\ruff.exe format scripts\validate_release_env.py tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check scripts\validate_release_env.py tests\test_release_config_p11.py`、Node REPL 直接执行 `.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 31 项、Node REPL 直接执行 `.venv\Scripts\mypy.exe app`、Node REPL 直接执行 `git diff --check` 通过；首次 ruff 检查只发现新测试中 `.encode("utf-8")` 可简化，已改为 `.encode()` 后复跑通过。

2026-06-25 继续加固发布 env 重复变量预检：`validate_release_env.py` 现在在解析阶段拒绝重复变量名，避免同一个 release env 中出现“最后一个值静默覆盖前一个值”的发布前歧义；新增回归覆盖重复 `JWT_SECRET` 只报告变量名、不打印任一候选 secret 值。本轮 `.venv\Scripts\ruff.exe format scripts\validate_release_env.py tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check scripts\validate_release_env.py tests\test_release_config_p11.py`、Node REPL 直接执行 `.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 32 项、Node REPL 直接执行 `.venv\Scripts\mypy.exe app`、Node REPL 直接执行 `git diff --check` 通过。

2026-06-25 继续加固发布 env 畸形行预检：`validate_release_env.py` 现在在解析阶段拒绝非注释非空但缺少 `=` 的畸形行，以及 `export NAME=value` 这类非法变量名写法，避免 Docker Compose 发布 env 文件与预检脚本理解不一致；非法变量名错误现在只报告行号，不回显左侧原文，避免 secret 被误写到 `=` 前时进入预检输出。新增回归覆盖错误只报告行号、不回显 secret-like 值。本轮 `.venv\Scripts\ruff.exe format scripts\validate_release_env.py tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check scripts\validate_release_env.py tests\test_release_config_p11.py`、Node REPL 直接执行 `.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 34 项、Node REPL 直接执行 `.venv\Scripts\mypy.exe app`、Node REPL 直接执行 `git diff --check` 通过。

2026-06-25 继续加固备份脚本 CRLF env 兼容：`backup-postgres.sh` 现在在 source release env 后清理 `POSTGRES_USER` 和 `POSTGRES_DB` 中的回车，避免 Windows 编辑过的 env 文件把 `\r` 传入 `pg_dump` 参数；新增回归用 CRLF env 文件和 fake `docker` 函数验证所有 docker/pg_dump 参数均不含回车，同时仍生成 dump 与 `.sha256`。本轮 `bash -n infra/scripts/backup-postgres.sh infra/scripts/restore-postgres.sh`、`.venv\Scripts\ruff.exe format tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check tests\test_release_config_p11.py`、Node REPL 直接执行 `.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 35 项、Node REPL 直接执行 `.venv\Scripts\mypy.exe app`、Node REPL 直接执行 `git diff --check` 通过；首次 ruff 检查只发现新增 Bash 片段单行过长，拆分后复跑通过。

2026-06-25 继续加固恢复脚本生产授权 CRLF 兼容：`restore-postgres.sh` 现在在判断 production restore guard 前清理 `RESTORE_ALLOW_PRODUCTION` 中的回车，与 `APP_ENV`、`POSTGRES_USER`、`POSTGRES_DB` 的 CRLF 处理保持一致；新增回归用 CRLF env 文件设置 `APP_ENV=production` 与 `RESTORE_ALLOW_PRODUCTION=1`，并用 fake `docker` 函数验证恢复路径能通过 guard 且 pg_restore 参数不含回车。本轮 `bash -n infra/scripts/backup-postgres.sh infra/scripts/restore-postgres.sh`、`.venv\Scripts\ruff.exe format tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check tests\test_release_config_p11.py`、Node REPL 直接执行 `.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 36 项、Node REPL 直接执行 `.venv\Scripts\mypy.exe app`、Node REPL 直接执行 `git diff --check` 通过。

2026-06-25 继续加固前端 dist 密钥扫描回归：`scan-dist-secrets.mjs` 增加仅用于本地测试的 `WEB_DIST_SCAN_DIR` 覆盖入口，默认仍扫描 `apps/web/dist`；新增 `scripts/scan-dist-secrets.test.ts`，用临时 dist 和真实环境变量值验证扫描失败时只报告 `contains value from JWT_SECRET`，不打印 secret 本身，同时验证干净 dist 通过。本轮继续扩展扫描器，增加 Aliyun OSS 与 S3-compatible signed URL marker 检测；回归证明硬编码 OSS signed URL 只报告 marker，不打印 URL 或签名值。本轮继续补齐 Aliyun OSS v1、Aliyun OSS v4、S3-compatible 三类签名 URL marker 回归，均验证只报告 marker、不打印 URL 或签名片段；最终通过 `cmd.exe /c` 执行 `pnpm --dir apps/web exec vitest run scripts/scan-dist-secrets.test.ts`，1 个文件 5 项通过；`pnpm --dir apps/web test -- scripts/scan-dist-secrets.test.ts` 实际跑过前端 13 个测试文件 47 项，其中 `scripts/scan-dist-secrets.test.ts` 5 项通过；`pnpm --dir apps/web lint`、`pnpm --dir apps/web type-check`、`pnpm --dir apps/web scan-dist-secrets`、`git diff --check` 通过。直接 PowerShell 跑前两条前端命令曾超时，未作为通过证据。

2026-06-25 继续补齐发布 manifest：新增 `services/backend/scripts/release_manifest.py`，用于发布前生成非敏感 JSON manifest，记录传入的 image tag、Git commit/short commit/worktree status、当前 Alembic head 和生产 Compose/Nginx/backend/web 发布产物 SHA-256；脚本支持 `--output` 将同一 JSON 写入调用方指定的验收证据文件，同时保留 stdout 输出，且不读取 `infra/env.production`、TLS 证书、备份 dump 或 secret env。`test_release_config_p11.py` 新增回归验证 manifest 记录 `0010_release_privacy`、保留传入 image tag、artifact hash 为 64 位、stdout 与保存文件一致、且不包含常见 secret env 名或私有 artifact 名。本轮 `.venv\Scripts\ruff.exe format scripts\release_manifest.py tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check scripts\release_manifest.py tests\test_release_config_p11.py`、`.venv\Scripts\mypy.exe app scripts\release_manifest.py`、目标 manifest 测试、`.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 37 项、`.venv\Scripts\python.exe scripts\release_manifest.py --image-tag thinking-coach:test-release-tag --output <temp-release-manifest.json>` 通过；临时输出文件确认包含 image tag、`0010_release_privacy` 和 5 个 artifact hash 后已删除。真实部署前仍需用实际 image tag 生成并保存 manifest 证据。
2026-06-25 继续加固发布 manifest 输出安全：`release_manifest.py` 现在拒绝空白 image tag、包含空白字符的 image tag、非 `.json` 输出路径、会覆盖被 hash 的发布源码/配置 artifact 的输出路径，以及 `infra/certs`、`backups`、`secrets` 等私有发布 artifact 路径；新增回归验证被拒绝的输出不会创建文件，也不会修改 `apps/web/package.json`。本轮 `.venv\Scripts\ruff.exe format scripts\release_manifest.py tests\test_release_config_p11.py`、`.venv\Scripts\ruff.exe check scripts\release_manifest.py tests\test_release_config_p11.py`、`.venv\Scripts\mypy.exe app scripts\release_manifest.py`、release manifest 目标测试 7 项、`.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 43 项通过。
2026-06-25 继续加固 worker 失败错误码脱敏：`app.workers.main._error_code()` 现在只保留符合 `[A-Z][A-Z0-9_]{0,63}` 的业务错误码；异常对象上的不安全 `code`/`error_code`（包含 proof code、refresh token、音频路径、签名 URL、signature 或 API key 形态）会退回异常类名，避免进入 job failure 字段。新增 `tests/test_worker_errors_p11.py` 覆盖安全码保留与敏感码拒绝。本轮 `.venv\Scripts\ruff.exe format app\workers\main.py tests\test_worker_errors_p11.py`、`.venv\Scripts\ruff.exe check app\workers\main.py tests\test_worker_errors_p11.py`、`.venv\Scripts\mypy.exe app`、`.venv\Scripts\pytest.exe -q tests\test_worker_errors_p11.py` 7 项通过；`tests\test_release_privacy_p11.py::test_account_deletion_failed_error_code_is_redacted_for_audit` 因当前 shell 未设置 `TEST_DATABASE_URL`/`TEST_DATABASE_SYNC_URL` 按预期 skip，未作为通过证据。
2026-06-25 继续补强 P11 临时数据库级隐私验收：启动临时 `pgvector/pgvector:pg16` 容器 `codex-p11-pg-1782380240000`，数据库名 `thinking_p11_test`，等待 `pg_isready` 后用该临时库设置 `TEST_DATABASE_URL`/`TEST_DATABASE_SYNC_URL`，执行 `.venv\Scripts\pytest.exe -q tests\test_migrations_p11.py tests\test_release_privacy_p11.py`，19 项通过；覆盖 P11 Alembic upgrade/downgrade/upgrade、report/export/appeal/account deletion、single-training deletion、proof-code status、deletion anonymization、source/user isolation 和 deletion failure redaction 集成路径。仅出现既有 Alembic `path_separator` deprecation warning；随后执行 `docker rm -f codex-p11-pg-1782380240000` 并用 `docker ps -a --filter name=codex-p11-pg-1782380240000 --format {{.Names}}` 确认无残留容器。该证据来自临时测试库，不等同于真实发布环境或目标设备验收。
2026-06-25 继续补齐备份/恢复本地门禁证据：重跑 `bash -n infra/scripts/backup-postgres.sh infra/scripts/restore-postgres.sh` 通过；重跑 `test_backup_script_prune_only_retains_daily_and_weekly_snapshots`、`test_restore_script_verifies_checksum_before_restore`、`test_restore_script_rejects_bad_checksum_before_env_or_docker`、`test_restore_script_verifies_checksum_against_selected_backup_path` 4 项通过，确认保留至少 7 个最新日备份与 4 个更旧周快照、保留手工 dump、删除匹配的陈旧 `.sha256` sidecar，并确认 restore 在 `pg_restore` 前校验 checksum、坏 hash 在读取 release env 或触达 Docker 前失败、sidecar hash 绑定到命令行选择的 dump 本身；整组 `.venv\Scripts\pytest.exe -q tests\test_release_config_p11.py` 43 项通过。该证据不等同于使用 approved release env 的目标环境 restore 演练。
2026-06-25 当前后端总门禁刷新记录：在 worker 错误码加固后重跑后端门禁，`uv lock --check` 在线模式失败，原因是 uv 解析 138 个包后要求把 `uv.lock` 的 registry URL/metadata 从 `mirrors.aliyun.com` 改写为 `pypi.tuna.tsinghua.edu.cn` 并补充 size/upload-time；这会产生 893 行无关锁文件噪音，已反向应用 diff 恢复，未提交锁文件变化，且未运行 `uv lock`。随后 `uv lock --check --offline` 通过；`uv run python scripts\verify_dependency_policy.py`、`.venv\Scripts\ruff.exe check .`、`.venv\Scripts\ruff.exe format --check .`、`.venv\Scripts\mypy.exe app`、`.venv\Scripts\pytest.exe -q` 均通过；pytest 仅保留既有 Starlette TestClient deprecation warning 和按环境跳过的数据库/live-service-gated 测试。P11 最终 signoff 仍需解决在线 `uv lock --check` 门禁，或明确批准将其记录为环境 registry metadata 问题。

2026-06-25 继续定位 P11 `uv lock --check` 在线门槛：当前 `cmd` 环境 `set UV` 返回无 `UV_*` 变量，裸 `uv lock --check` 解析 138 个包后退出 1，并提示 lockfile 需要更新，但本次确认 `services/backend/uv.lock` 无状态变化、无 diff；显式执行 `set UV_DEFAULT_INDEX=https://mirrors.aliyun.com/pypi/simple&& uv lock --check` 退出 0，`git status --short -- services/backend/uv.lock` 为空，证明与当前 Aliyun registry 锁文件一致；显式 `UV_INDEX_URL=https://mirrors.aliyun.com/pypi/simple` 不等价，仍退出 1。未运行 `uv lock`，未接受任何锁文件重写；P11 后续发布 shell 应使用 `UV_DEFAULT_INDEX` 固定后的在线锁文件门槛。

2026-06-25 重新尝试推进 P11 独立 `/review`：已读取 `C:\Users\WJ\.codex\skills\gstack-review\SKILL.md`，该技能明确要求当前工具列表必须存在 `AskUserQuestion` 变体，否则按技能规则停止并报告 blocked；当前宿主仅有 Plan mode 专用 `request_user_input`，不满足该技能要求，因此未执行交互式 gstack `/review`，也未记录为通过证据。P11 仍需在具备 AskUserQuestion 的宿主运行独立 `/review`，或由用户明确批准非交互式替代。

## P10 实机补充验证

2026-06-24 使用用户 USB 连接的 vivo X100 Pro 继续测试手机 Chrome：设备 `10AE2M0P60002R5` 在线，Chrome `130.0.6723.102`、GMS `26.23.34` 存在，ADB reverse 已映射 `5182/8010/5183`，本地 web/API/debug 页均可访问。Chrome 在 `http://127.0.0.1:5183/` 和 `http://localhost:5183/` 均显示 `isSecureContext=true`、`hasServiceWorker=true`、`hasPushManager=true`、`hasNotification=true`，通知权限可授予到 `granted`；但手机直连时 `PushManager.subscribe()` 两次均返回 `AbortError: Registration failed - push service error`。网络探测显示手机可连 `mtalk.google.com:5228`，但 `fcmregistrations.googleapis.com:443`、`firebaseinstallations.googleapis.com:443`、`android.clients.google.com:443` 均超时；电脑同一时间可连这些端点，且本机有 `verge-mihomo` 监听 `127.0.0.1:7897`。

用户确认允许后，临时设置手机全局代理 `127.0.0.1:7897` 并保留 ADB reverse `tcp:7897`。强制重启手机 Chrome 后，debug 页 `http://localhost:5183/` 成功生成真实 FCM `PushSubscription`；正式 PWA `http://localhost:5182/` 也通过 CDP 重新登录测试账号、解除旧订阅、用 raw VAPID 测试公钥重新订阅并保存，后端返回 `saveStatus=201`、`savedActive=true`。当前测试库为 `thinking_mumu_p10_firefox_20260624_182332`。

真实发送验证分三段：追修前直接使用 `PyWebPushSender` 时，Windows/代理 fake-IP DNS 让 `fcm.googleapis.com` 解析到 198.18/15，发送前安全检查返回 `WEB_PUSH_ENDPOINT_BLOCKED`，会话 `ecf11f43-c674-4f26-b3e7-3f1bbbb39c59` 进入 `EXPIRED/FAILED`；随后用受控本地 smoke 脚本临时跳过 `_validate_resolved_endpoint_addresses()` 并设置 `HTTPS_PROXY=http://127.0.0.1:7897`，`RandomStrikeService.send_due_notifications()` 对 session `beeb65a9-b952-46e5-a3e0-bc8fa5493ce0` 返回 `sent_count=1`，DB 为 `NOTIFIED/SUCCEEDED`；追修后新增默认关闭的 `WEB_PUSH_ALLOW_FAKE_IP_HOSTS` 配置并设置 `fcm.googleapis.com`，不再 monkeypatch，直接调用真实 `PyWebPushSender` 返回 `delivered=True`、`error_code=None`。手机 `dumpsys notification` 出现 Chrome Web 通知，tag 为 `strike:beeb65a9-b952-46e5-a3e0-bc8fa5493ce0` 和 `p10-codefix:134725`，标题 `突击审核已到达`。测试后确认手机 `http_proxy=null`。

追修后用当前代码重新创建并发送 session `3a1f5178-8d99-4629-a156-60c398b3e659`：通知到达手机 Chrome；接受前 DB 为 `NOTIFIED`、题目 `READY/exposed_count=0`，PWA 显示 `突击已到达` 且 `开始录音` disabled；点击接受后，DB 为 `WAIT_FIRST_AUDIO`、题目 `EXPOSED/exposed_count=1`，PWA 显示真实中文题干与 `开始录音` enabled。经用户允许临时放开 Chrome 录音 AppOp 后，使用手机真实点击完成 FIRST、FOLLOWUP、FINAL 三段录音上传；三段 `voice_attempt` 均为 `UPLOADED`，三段 transcript 均为 `SUCCEEDED`，单步运行现有 mock worker 处理三个 `GRAPH_RESUME` job 和一个 `EVALUATE_SESSION` job 后，session 最终 `COMPLETED`，评审报告 `COMPLETED`（logic 100、speech 80、adaptability 70、final 90）。测试后确认手机 `http_proxy=null`，DevTools forward 已移除，Chrome `RECORD_AUDIO` UID 级 AppOp 已恢复为 `ignore`；Android 仍保留包级 `RECORD_AUDIO: allow` 历史条目，未执行整包 reset 以避免重置 Chrome 其他权限。

2026-06-24 继续补测完成页恢复兜底：本地创建假账号 `p10-restore-phone-2240` 和 completed session `659951a4-6a28-4533-a0f1-b61794a8fd55`；API 验证登录 200、`GET /api/v1/trainings/current` 返回 404、`GET /api/v1/trainings/{id}/state` 返回 `COMPLETED` 且带 `source_summary`、`GET /api/v1/trainings/{id}/provenance` 返回 200。手机 Chrome 打开 `http://localhost:5182/?session=659951a4-6a28-4533-a0f1-b61794a8fd55` 后登录，页面成功从 URL session 恢复完成态，显示 `本轮答辩已完成`、`下一条` 和来源卡片。

2026-06-24 继续补测通知点击：早期只点标题/正文节点时，session `3a1f5178-8d99-4629-a156-60c398b3e659` 的真实 Push 通知和 tag `p10-click-debug-1782312911049` 的 PWA 生成通知都出现过“通知被消费但停留在 Launcher”。随后解析通知栏 XML，找到 `P10area` 的可点击祖先 `com.android.systemui:id/expandableNotificationRow`，bounds 为 `[56,557][1204,799]`，点击行内中上部 `(630,650)` 后，通知被消费且 `topResumedActivity` 变为 `com.android.chrome/com.google.android.apps.chrome.Main`。再次生成带唯一参数的 PWA Web 通知 `P10probe`，tag `p10-probe-probe1782313404182`，可点击行 bounds 为 `[56,1129][1204,1371]`，点击 `(630,1221)` 后 Chrome 被唤起；手机 UI 地址栏显示 `localhost:5182/?session=659951a4-6a28-4533-a0f1-b61794a8fd55&click_probe=probe1782313404182`，证明通知 `data.url` 能落到目标 PWA URL。限制：MuMu 内的网页 `PushSubscription` 正向环境仍不可用。

2026-06-24 用户体验追修：前端新增 `/api/v1/auth/refresh` 调用，登录成功后保存 refresh token，重新打开 PWA 时静默轮换 token 并恢复当前/完成训练状态，避免只因 `sessionStorage` 丢失就反复要求登录；测试夹具和 Mock Provider 默认题目不再使用“请基于材料/材料显示”这类暗示隐藏材料的文案，改为在题目中直接给出“已知事实”，完整来源仍按 SPEC/SOP 在完成后公开。

2026-06-24 验收口径变更：用户明确要求“不用 MuMu 模拟器测试了，使用 APP，先忽略 MuMu 模拟器的验证”。因此 P10 按已连接 vivo X100 Pro + Chrome PWA/App 真机链路标记为 `VERIFIED`；MuMu 正向 Push 问题仅作为后续兼容风险保留。
